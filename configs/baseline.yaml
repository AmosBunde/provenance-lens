# Baseline configuration: frozen backbone, trainable head, sweep grid.
backbone: resnet50
weights: IMAGENET1K_V2

head:
  hidden: [512, 128]
  dropout: 0.3

train:
  batch_size: 512
  max_epochs: 40
  patience: 5
  seed: 20260803

sweep:
  learning_rates: [0.0003, 0.001, 0.003, 0.01]
